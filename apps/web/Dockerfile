FROM node:22.13.1-bookworm-slim AS build

WORKDIR /app

ARG NODE_OPTIONS
ARG VITE_API_BASE
ARG VITE_API_URL
ARG VITE_ENABLE_TEMPLATE_DESIGNER
ARG VITE_FIREBASE_API_KEY
ARG VITE_FIREBASE_APP_ID
ARG VITE_FIREBASE_AUTH_DOMAIN
ARG VITE_FIREBASE_PROJECT_ID

ENV NODE_OPTIONS=$NODE_OPTIONS
ENV VITE_API_BASE=$VITE_API_BASE
ENV VITE_API_URL=$VITE_API_URL
ENV VITE_ENABLE_TEMPLATE_DESIGNER=$VITE_ENABLE_TEMPLATE_DESIGNER
ENV VITE_FIREBASE_API_KEY=$VITE_FIREBASE_API_KEY
ENV VITE_FIREBASE_APP_ID=$VITE_FIREBASE_APP_ID
ENV VITE_FIREBASE_AUTH_DOMAIN=$VITE_FIREBASE_AUTH_DOMAIN
ENV VITE_FIREBASE_PROJECT_ID=$VITE_FIREBASE_PROJECT_ID
ENV VITE_GA_MEASUREMENT_ID=__VITE_GA_MEASUREMENT_ID__
ENV VITE_META_PIXEL_ID=__VITE_META_PIXEL_ID__
ENV VITE_PUBLIC_WHATSAPP_NUMBER=__VITE_PUBLIC_WHATSAPP_NUMBER__

COPY package.json ./
RUN npm install

COPY index.html ./
COPY vite.config.ts ./
COPY tsconfig.json ./
COPY tailwind.config.js ./
COPY postcss.config.js ./
COPY eslint.config.js ./
COPY serve.json ./
COPY public ./public
COPY src ./src
RUN npm run build

FROM node:22.13.1-bookworm-slim AS runtime

WORKDIR /app

ENV NODE_ENV=production
RUN npm install --global serve@14.2.4

COPY --from=build /app/dist ./dist

ENTRYPOINT ["sh", "-c", "echo \"[runtime-env] analytics env present: ga=${VITE_GA_MEASUREMENT_ID:+yes} meta=${VITE_META_PIXEL_ID:+yes} whatsapp=${VITE_PUBLIC_WHATSAPP_NUMBER:+yes}\" && find dist -type f \\( -name '*.html' -o -name '*.js' -o -name '*.css' \\) -exec sed -i \"s|__VITE_GA_MEASUREMENT_ID__|${VITE_GA_MEASUREMENT_ID}|g;s|__VITE_META_PIXEL_ID__|${VITE_META_PIXEL_ID}|g;s|__VITE_PUBLIC_WHATSAPP_NUMBER__|${VITE_PUBLIC_WHATSAPP_NUMBER}|g\" {} + && exec \"$@\"", "sh"]
CMD ["sh", "-c", "serve -s dist --single -l ${PORT:-3000}"]
