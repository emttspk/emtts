{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build --workspace=@labelgen/api && npm run build --workspace=@labelgen/web"
  },
  "deploy": {
    "startCommand": "npx serve -s dist --single -l $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
