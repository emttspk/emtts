{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "command": "npm install --omit=dev && npm run build"
  },
  "deploy": {
    "startCommand": "npm run build && npx serve dist -s -l $PORT"
  }
}
