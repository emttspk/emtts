node_modules
dist
.venv/
.env
.env.*
!.env.example
!.env.project.example
!.env.staging.local.example
.DS_Store
coverage

# Ignore only the dynamic data storage, not source code
apps/api/storage
outputs/

# Explicitly keep committed templates (must reach the deployed container)
!apps/api/templates/

# Runtime logs
*.log
*.err.log
*.out.log

# Local manual database backups
backups/
*.dump
*.sql
*.bak

# Temporary / smoke-test outputs
tmp-*.pdf
smoke-*.pdf
.smoke_tmp*/

# Generated runtime validation outputs and Puppeteer artifacts
storage/generated/
forensic-artifacts/
apps/api/backups/
telemetry-staging.log
temp-*.json
temp-*.mjs
temp-*.ts
apps/api/temp-*.json
apps/api/temp-*.mjs
apps/api/temp-*.cjs
**/render-proof-*/
**/render-proof-*.html
**/render-proof-*.pdf
**/render-proof-*.png
**/premium-envelope*.html
**/premium-envelope*.pdf
**/premium-envelope*.png
**/money-order-benchmark*.html
**/money-order-benchmark*.pdf
**/money-order-benchmark*.png
**/live-*-runtime.*
**/mo-static-front-proof.*
**/money-order-final-runtime.*
**/tracking-workspace-cache.*

# Excel / CSV test artifacts (local only)
LCS*.xls
LCS*.xlsx

tracking/credentials.json
EP Gateway/
EP Gateway/.venv/
.aider*

# Local-only operational documentation (bootstrap, audits, rollout, recovery)
# Machine-specific findings, execution reports, and deployment guides stay local
.local-docs/

# Additions for untracked files and local-only artifacts
# Exclude tasks.json (local-only task runner config)
tasks.json

# Exclude ISOLATION_STRATEGY.md (local-only documentation strategy)
ISOLATION_STRATEGY.md

# One-off maintenance and migration helpers
scripts/tmp-*.js
scripts/tmp-*.mjs
scripts/tmp-*.ts
scripts/tmp-*.cjs

# Transient maintenance snapshots (retain only curated final docs/manifests)
MAINTENANCE-SNAPSHOT-TEMP*.md

# Internal documentation and verification artifacts must remain local-only
CHANGELOG.md
IMPLEMENTATION_NOTES.md
forensic-artifacts/
docs/internal/
*_PLAN.md
*_NOTES.md
*_GUIDE.md
verification-*.json
strict-runtime-verify-*.json
retention-cleanup-verify-*.json

# Local-only audit and UI review artifacts (never commit)
docs/local-ui-audit/
audit/
reports/
screenshots/
tmp-audit/

# Local third-party reference bundle (not part of product source)
jazz cash/

# Project signature guard secret protection
.ai-project/private/
*.pem
*.key
service-account*.json
*credentials*.json
railway-secrets
cloudflare-secrets

# Raw marketing keyword exports (local only)
marketing/*.csv
