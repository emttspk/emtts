[phases.setup]
nixPkgs = ["pkg-config", "cairo", "pango", "libpng", "libjpeg", "giflib", "librsvg", "pixman", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm cache clean --force", "rm -rf node_modules apps/api/node_modules apps/web/node_modules", "npm install --include=optional --no-audit --build-from-source"]

[phases.build]
cmds = ["npm run prisma:generate -w apps/api", "npm rebuild canvas --build-from-source", "npm run build -w apps/api"]

[variables]
PUPPETEER_SKIP_DOWNLOAD = "true"
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
NPM_CONFIG_BUILD_FROM_SOURCE = "true"