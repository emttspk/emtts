{
  "name": "p-post-label-generator",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:web": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "worker": "npm run worker -w apps/api",
    "worker:dev": "npm run worker:dev -w apps/api",
    "dev:all": "concurrently \"npm run dev -w apps/api\" \"npm run worker:dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build:web": "cd apps/web && npm install && npm run build",
    "start:web": "cd apps/web && npm run start",
    "start": "node apps/api/dist/index.js",
    "build": "npm run build --workspace=@labelgen/web && npm run build --workspace=@labelgen/api",
    "build:all": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "concurrently": "^9.2.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "prisma": {
    "schema": "apps/api/prisma/schema.prisma"
  }
}
