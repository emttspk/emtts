{
  "name": "p-post-label-generator",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev:web": "npm run preview --workspace=@labelgen/web",
    "dev:api": "npm run dev --workspace=@labelgen/api",
    "worker": "npm run worker --workspace=@labelgen/api",
    "worker:dev": "npm run worker:dev --workspace=@labelgen/api",
    "dev:all": "concurrently \"npm run dev --workspace=@labelgen/api\" \"npm run worker:dev --workspace=@labelgen/api\" \"npm run preview --workspace=@labelgen/web\"",
    "build:web": "npm run build --workspace=@labelgen/web",
    "start:web": "cd apps/web && npm run start",
    "start": "npm run start --workspace=@labelgen/api",
    "build": "npm run build --workspace=@labelgen/api && npm run build --workspace=@labelgen/web",
    "build:all": "npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "concurrently": "^9.2.1",
    "puppeteer": "^22.15.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "prisma": {
    "schema": "apps/api/prisma/schema.prisma"
  }
}
